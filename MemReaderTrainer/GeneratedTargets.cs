namespace MemReader;

/// <summary>
/// Placeholder so this project builds on its own. MainForm's "Build Trainer" step
/// overwrites this file with the real process name and targets in a private copy of
/// this project before publishing - the checked-in version never ships.
/// </summary>
internal static class GeneratedTargets
{
    public const string ProcessName = "notepad";

    public static readonly TrainerTarget[] Targets =
    {
        new("Example (edit me)", ValueKind.Int32, 4, null, 0, Array.Empty<int>(), 0, null, null, null, 0),
    };
}
