# Build output - regenerated by `dotnet build`/`dotnet publish`, never source.
bin/
obj/
dist/

# Local crash logs and per-machine settings.
*.user
crash.log

# Pointer-chain exports are per-target and often per-machine; keep them local.
*.chains
